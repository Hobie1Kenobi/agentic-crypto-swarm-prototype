# x402 Strategy Dashboard bundle

Thank you for purchasing. This package includes:

- `static/index.html` — browser viewer for the included catalog snapshot
- `static/data/catalog-slim.json` — curated provider rows (HTTPS, agent-callable, trust-filtered)
- `LICENSE` — your usage terms

## View

From the `static` directory:

```bash
python -m http.server 8766
```

Then open `http://127.0.0.1:8766/` in a browser.

Opening `index.html` directly from disk may block loading JSON; use a local server.

## Disclaimers

- Catalog fields (price, trust, latency) come from public discovery sources and may change.
- Listing a service does not guarantee availability, pricing, or permission to resell outputs.
- Compliance, AML, and security-adjacent workflows may be regulated; obtain your own legal advice.

## Support

Use the contact channel provided at purchase for updates or integration questions.
